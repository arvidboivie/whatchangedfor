import { sharedParserInterface } from './shared-parser-interface';

describe('sharedParserInterface', () => {
  it('should work', () => {
    expect(sharedParserInterface()).toEqual('shared-parser-interface');
  });
});
